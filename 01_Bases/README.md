# 📚 Module 1 : Bases Essentielles Python

Ce module couvre les fondations nécessaires pour écrire des scripts Python solides. Contrairement à Bash, Python est un langage fortement typé (mais dynamiquement) et structuré par l'indentation.

## 🎯 Objectifs
- Comprendre la philosophie de Python (PEP 8).
- Maîtriser les types de données et les structures fondamentales.
- Apprendre à gérer les erreurs proprement.

## 📂 Contenu du Module

### 1. Syntaxe et Premiers Pas (`01_syntaxe.py`)
- L'importance de l'indentation (4 espaces).
- Les commentaires.
- La fonction `print()`.

### 2. Variables et Types (`02_variables.py`)
- Types scalaires : `int`, `float`, `str`, `bool`.
- Typage dynamique.
- f-strings : La méthode moderne de formatage de chaînes.

### 3. Structures de Données (`03_structures.py`)
- **Listes** (`[]`) : Collections ordonnées et modifiables.
- **Tuples** (`()`) : Collections ordonnées et immuables.
- **Dictionnaires** (`{}`) : Clé-Valeur (HashMaps).
- **Sets** (`{}`) : Collections uniques non ordonnées.

### 4. Gestion des Erreurs (`04_exceptions.py`)
- Blocs `try`, `except`, `else`, `finally`.
- Ne jamais laisser un programme planter silencieusement.

## 🚀 Comment exécuter les scripts ?
Ouvrez votre terminal dans ce dossier et lancez :
```bash
python 01_syntaxe.py
python 02_variables.py
# etc.
```
