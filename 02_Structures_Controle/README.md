# 🔀 Module 2 : Structures de Contrôle

Automatiser, c'est prendre des décisions et répéter des tâches. Ce module vous apprend à maîtriser le flux d'exécution de vos scripts.

## 🎯 Objectifs
- Écrire des conditions complexes (`if/elif/else`).
- Maîtriser les boucles et l'itération (`for`, `while`).
- Utiliser les **List Comprehensions** (la marque des pros en Python).

## 📂 Contenu du Module

### 1. Conditions (`01_conditions.py`)
- Opérateurs logiques : `and`, `or`, `not`.
- Comparaisons multiples (ex: `10 < x < 20`).
- Structures `if` imbriquées.

### 2. Boucles (`02_boucles.py`)
- Boucle `for` : Le standard pour itérer sur des collections.
- Boucle `while` : Pour répéter tant qu'une condition est vraie.
- Mots-clés `break` (sortir) et `continue` (passer au suivant).
- Fonctions utiles : `range()`, `enumerate()`, `zip()`.

### 3. Les Comprehensions (`03_comprehensions.py`)
- Transformer une boucle de 5 lignes en une seule ligne élégante.
- Filtrer des données à la volée.
- S'applique aux listes et dictionnaires.

## 💡 Le Conseil du Pro
En Python, on préfère itérer directement sur les éléments (`for item in liste`) plutôt que sur les index (`for i in range(len(liste))`). C'est plus lisible et plus performant.
